#  this is now a third file
#   09:02 27 April 2011
#  It does just as little as the other files do

max(runif(100))

# this could be turned into a histogram as well

#  might as well provide the histogram then
hist(runif(100))